name: data
description: Data module
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.2.0

# Add regular dependencies here.
dependencies:
  domain:
    path: ../domain
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.9
